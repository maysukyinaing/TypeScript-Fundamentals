var courseName = "TypeScript Bootcamp";
debugger;
if (courseName) {
    var subtitle = "Learn the language from ground up!";
    printCourseName(courseName);
}
function printCourseName(name) {
    debugger;
    console.log("The Course name is : " + name.toUpperCase());
}
